const router = require('express').Router();
const { withOrg } = require('../utils/orgScopedDb');
const UUID_RE = /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
const STATUSES = new Set(['draft','pending_verification','verified','active','suspended','revoked']);
const LIST_STATUSES = new Set(['active','suspended','archived']);
const CHANNELS = new Set(['email','sms','whatsapp','portal','web']);
const MODES = new Set(['immediate','digest','batched']);
const DOMAINS = new Set(['platform','fleet','cds']);
function actor(req){return req.user?.id||req.user?.user_id||null;}
function uuidError(value,name){return value==null||UUID_RE.test(String(value))?null:`"${name}" must be a valid UUID`;}
function httpError(message,status=400){const e=new Error(message);e.status=status;return e;}
async function audit(c,req,event){await c.query(`INSERT INTO communication_authority_audit (org_id,actor_user_id,action,entity_type,entity_id,scope_domain,scope_client_id,scope_cds_customer_id,before_state,after_state,metadata) VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9::jsonb,$10::jsonb,$11::jsonb)`,[req.user.org_id,actor(req),event.action,event.entityType,event.entityId||null,event.domain||null,event.clientId||null,event.cdsCustomerId||null,event.before==null?null:JSON.stringify(event.before),event.after==null?null:JSON.stringify(event.after),JSON.stringify(event.metadata||{})]);}
function validateEnrollment(body){const allowed=new Set(['recipient_id','domain','client_id','cds_customer_id','contact_role','locale','timezone','status']);const unknown=Object.keys(body||{}).filter(k=>!allowed.has(k));if(unknown.length)return `"${unknown[0]}" is not allowed`;for(const key of ['recipient_id','client_id','cds_customer_id']){const e=uuidError(body[key],key);if(e)return e;}if(!body.recipient_id)return '"recipient_id" is required';if(!DOMAINS.has(body.domain))return '"domain" must be one of [platform, fleet, cds]';if(body.status!=null&&!STATUSES.has(body.status))return 'invalid enrollment status';if(body.domain==='cds'&&!body.cds_customer_id)return '"cds_customer_id" is required for CDS enrollment';if(body.domain==='cds'&&body.client_id!=null)return '"client_id" is not allowed for CDS enrollment';if(body.domain==='fleet'&&body.cds_customer_id!=null)return '"cds_customer_id" is not allowed for Fleet enrollment';if(body.domain==='platform'&&(body.client_id!=null||body.cds_customer_id!=null))return '"client_id" and "cds_customer_id" are not allowed for platform enrollment';return null;}
router.get('/recipients',async(req,res,next)=>{try{const q=await withOrg(req.user.org_id,c=>c.query(`SELECT r.id,r.email,r.name,r.company,r.enabled,r.authority_role,r.client_id,COALESCE(json_agg(json_build_object('id',e.id,'domain',e.domain,'client_id',e.client_id,'cds_customer_id',e.cds_customer_id,'contact_role',e.contact_role,'locale',e.locale,'timezone',e.timezone,'status',e.status,'subscriptions',COALESCE((SELECT json_agg(json_build_object('id',s.id,'event_type',s.event_type,'channel',s.channel,'enabled',s.enabled,'delivery_mode',s.delivery_mode,'critical_override',s.critical_override) ORDER BY s.event_type,s.channel) FROM communication_subscriptions s WHERE s.enrollment_id=e.id),'[]'::json))) ORDER BY e.created_at) FILTER(WHERE e.id IS NOT NULL),'[]'::json) enrollments FROM client_email_recipients r LEFT JOIN communication_enrollments e ON e.org_id=r.org_id AND e.recipient_id=r.id WHERE r.org_id=$1 AND r.deleted_at IS NULL GROUP BY r.id ORDER BY lower(COALESCE(r.name,r.email)),lower(r.email)`,[req.user.org_id]));res.json({data:q.rows});}catch(e){next(e);}});
router.patch('/recipients/:id',async(req,res,next)=>{try{const allowed=new Set(['email','name','company','enabled']);const unknown=Object.keys(req.body||{}).filter(k=>!allowed.has(k));if(unknown.length)return res.status(400).json({error:`"${unknown[0]}" is not allowed`});const out=await withOrg(req.user.org_id,async c=>{const b=await c.query(`SELECT id,email,name,company,enabled FROM client_email_recipients WHERE id=$1 AND org_id=$2 AND deleted_at IS NULL`,[req.params.id,req.user.org_id]);if(!b.rows.length)throw httpError('recipient_not_found',404);const u=await c.query(`UPDATE client_email_recipients SET email=COALESCE($2,email),name=COALESCE($3,name),company=COALESCE($4,company),enabled=COALESCE($5,enabled),updated_at=NOW() WHERE id=$1 AND org_id=$6 RETURNING id,email,name,company,enabled,authority_role,client_id`,[req.params.id,req.body.email==null?null:String(req.body.email).trim().toLowerCase(),req.body.name??null,req.body.company??null,req.body.enabled==null?null:req.body.enabled!==false,req.user.org_id]);await audit(c,req,{action:'recipient.updated',entityType:'recipient',entityId:req.params.id,before:b.rows[0],after:u.rows[0]});return u.rows[0];});res.json({data:out});}catch(e){next(e);}});
router.post('/recipients',async(req,res,next)=>{try{const{email,name=null,company=null,enabled=true}=req.body||{};if(!email||!String(email).trim())return res.status(400).json({error:'email is required'});const out=await withOrg(req.user.org_id,async c=>{const q=await c.query(`INSERT INTO client_email_recipients(org_id,email,name,company,enabled) VALUES($1,$2,$3,$4,$5) ON CONFLICT(org_id,email) DO UPDATE SET name=EXCLUDED.name,company=EXCLUDED.company,enabled=EXCLUDED.enabled,updated_at=NOW(),deleted_at=NULL RETURNING id,org_id,email,name,company,enabled,client_id,authority_role`,[req.user.org_id,String(email).trim().toLowerCase(),name,company,enabled!==false]);await audit(c,req,{action:'recipient.created_or_restored',entityType:'recipient',entityId:q.rows[0].id,after:q.rows[0]});return q.rows[0];});res.status(201).json({data:out});}catch(e){next(e);}});
router.post('/enrollments',async(req,res,next)=>{try{const error=validateEnrollment(req.body);if(error)return res.status(400).json({error,details:error});const{recipient_id,domain,client_id=null,cds_customer_id=null,contact_role=null,locale=null,timezone=null,status='pending_verification'}=req.body;const out=await withOrg(req.user.org_id,async c=>{const r=await c.query(`SELECT id FROM client_email_recipients WHERE id=$1 AND org_id=$2 AND deleted_at IS NULL`,[recipient_id,req.user.org_id]);if(!r.rows.length)throw httpError('recipient_not_found',404);if(client_id){const q=await c.query(`SELECT id FROM cargo_clients WHERE id=$1 AND org_id=$2 AND deleted_at IS NULL`,[client_id,req.user.org_id]);if(!q.rows.length)throw httpError('client_not_found',404);}if(cds_customer_id){const q=await c.query(`SELECT id FROM cds_customers WHERE id=$1 AND org_id=$2 AND deleted_at IS NULL`,[cds_customer_id,req.user.org_id]);if(!q.rows.length)throw httpError('cds_customer_not_found',404);}const existing=await c.query(`SELECT * FROM communication_enrollments WHERE org_id=$1 AND recipient_id=$2 AND domain=$3 AND ((domain='cds' AND cds_customer_id=$4::uuid) OR (domain='fleet' AND client_id=$5::uuid) OR domain='platform') LIMIT 1`,[req.user.org_id,recipient_id,domain,cds_customer_id,client_id]);let row;if(existing.rows.length){const u=await c.query(`UPDATE communication_enrollments SET client_id=$2,contact_role=$3,locale=$4,timezone=$5,status=$6,updated_at=NOW() WHERE id=$1 RETURNING *`,[existing.rows[0].id,client_id,contact_role,locale,timezone,status]);row=u.rows[0];await audit(c,req,{action:'enrollment.updated',entityType:'enrollment',entityId:row.id,domain,clientId:client_id,cdsCustomerId:cds_customer_id,before:existing.rows[0],after:row});}else{const i=await c.query(`INSERT INTO communication_enrollments(org_id,recipient_id,domain,client_id,cds_customer_id,contact_role,locale,timezone,status) VALUES($1,$2,$3,$4,$5,$6,$7,$8,$9) RETURNING *`,[req.user.org_id,recipient_id,domain,client_id,cds_customer_id,contact_role,locale,timezone,status]);row=i.rows[0];await audit(c,req,{action:'enrollment.created',entityType:'enrollment',entityId:row.id,domain,clientId:client_id,cdsCustomerId:cds_customer_id,after:row});}if(client_id)await c.query(`UPDATE client_email_recipients SET client_id=$2,updated_at=NOW() WHERE id=$1 AND org_id=$3`,[recipient_id,client_id,req.user.org_id]);return row;});res.status(201).json({data:out});}catch(e){next(e);}});
router.get('/enrollments',async(req,res,next)=>{try{const q=await withOrg(req.user.org_id,c=>c.query(`SELECT * FROM communication_enrollments WHERE org_id=$1 ORDER BY created_at DESC`,[req.user.org_id]));res.json({data:q.rows});}catch(e){next(e);}});
router.patch('/enrollments/:id',async(req,res,next)=>{try{const allowed=new Set(['status','contact_role','locale','timezone']);const unknown=Object.keys(req.body||{}).filter(k=>!allowed.has(k));if(unknown.length)return res.status(400).json({error:`"${unknown[0]}" is not allowed`});if(req.body.status!=null&&!STATUSES.has(req.body.status))return res.status(400).json({error:'invalid enrollment status'});const out=await withOrg(req.user.org_id,async c=>{const b=await c.query(`SELECT * FROM communication_enrollments WHERE id=$1 AND org_id=$2`,[req.params.id,req.user.org_id]);if(!b.rows.length)throw httpError('enrollment_not_found',404);const u=await c.query(`UPDATE communication_enrollments SET status=COALESCE($2,status),contact_role=COALESCE($3,contact_role),locale=COALESCE($4,locale),timezone=COALESCE($5,timezone),revoked_at=CASE WHEN $2='revoked' THEN COALESCE(revoked_at,NOW()) ELSE revoked_at END,suspended_at=CASE WHEN $2='suspended' THEN COALESCE(suspended_at,NOW()) ELSE suspended_at END,updated_at=NOW() WHERE id=$1 AND org_id=$6 RETURNING *`,[req.params.id,req.body.status??null,req.body.contact_role??null,req.body.locale??null,req.body.timezone??null,req.user.org_id]);await audit(c,req,{action:`enrollment.${req.body.status||'updated'}`,entityType:'enrollment',entityId:req.params.id,domain:b.rows[0].domain,clientId:b.rows[0].client_id,cdsCustomerId:b.rows[0].cds_customer_id,before:b.rows[0],after:u.rows[0]});return u.rows[0];});res.json({data:out});}catch(e){next(e);}});
router.put('/enrollments/:id/subscriptions',async(req,res,next)=>{try{const subs=Array.isArray(req.body?.subscriptions)?req.body.subscriptions:[];if(!subs.length)return res.status(400).json({error:'subscriptions must contain at least one item'});const out=await withOrg(req.user.org_id,async c=>{const e=await c.query(`SELECT * FROM communication_enrollments WHERE id=$1 AND org_id=$2`,[req.params.id,req.user.org_id]);if(!e.rows.length)throw httpError('enrollment_not_found',404);for(const s of subs){if(!s?.event_type||!CHANNELS.has(s.channel||'email')||!MODES.has(s.delivery_mode||'immediate'))throw httpError('invalid subscription payload');await c.query(`INSERT INTO communication_subscriptions(org_id,enrollment_id,event_type,channel,delivery_mode,enabled,critical_override) VALUES($1,$2,$3,$4,$5,$6,$7) ON CONFLICT(enrollment_id,event_type,channel) DO UPDATE SET delivery_mode=EXCLUDED.delivery_mode,enabled=EXCLUDED.enabled,critical_override=EXCLUDED.critical_override,updated_at=NOW()`,[req.user.org_id,req.params.id,String(s.event_type),s.channel||'email',s.delivery_mode||'immediate',s.enabled!==false,s.critical_override!==false]);}const rows=await c.query(`SELECT * FROM communication_subscriptions WHERE enrollment_id=$1 ORDER BY event_type,channel`,[req.params.id]);await audit(c,req,{action:'subscription.updated',entityType:'enrollment',entityId:req.params.id,domain:e.rows[0].domain,clientId:e.rows[0].client_id,cdsCustomerId:e.rows[0].cds_customer_id,metadata:{subscriptions:subs.map(s=>({event_type:s.event_type,channel:s.channel||'email',enabled:s.enabled!==false}))},after:rows.rows});return rows.rows;});res.json({data:out});}catch(e){next(e);}});
router.get('/distribution-lists',async(req,res,next)=>{try{const params=[req.user.org_id];let filter='';if(req.query.cds_customer_id){const e=uuidError(req.query.cds_customer_id,'cds_customer_id');if(e)return res.status(400).json({error:e});params.push(req.query.cds_customer_id);filter=' AND l.cds_customer_id=$2';}const q=await withOrg(req.user.org_id,c=>c.query(`SELECT l.id,l.name,l.description,l.domain,l.client_id,l.cds_customer_id,l.status,l.created_at,l.updated_at,COALESCE(json_agg(json_build_object('id',m.id,'enrollment_id',m.enrollment_id,'recipient_id',e.recipient_id,'name',r.name,'email',r.email,'contact_role',e.contact_role,'status',e.status)) FILTER(WHERE m.id IS NOT NULL),'[]'::json) members FROM communication_distribution_lists l LEFT JOIN communication_distribution_list_members m ON m.distribution_list_id=l.id AND m.org_id=l.org_id LEFT JOIN communication_enrollments e ON e.id=m.enrollment_id AND e.org_id=l.org_id LEFT JOIN client_email_recipients r ON r.id=e.recipient_id AND r.org_id=l.org_id AND r.deleted_at IS NULL WHERE l.org_id=$1${filter} GROUP BY l.id ORDER BY lower(l.name)`,params));res.json({data:q.rows});}catch(e){next(e);}});
router.post('/distribution-lists',async(req,res,next)=>{try{const{name,description=null,domain='cds',client_id=null,cds_customer_id=null}=req.body||{};if(!name||!String(name).trim())return res.status(400).json({error:'name is required'});if(!DOMAINS.has(domain))return res.status(400).json({error:'invalid domain'});for(const[k,v]of[['client_id',client_id],['cds_customer_id',cds_customer_id]]){const e=uuidError(v,k);if(e)return res.status(400).json({error:e});}if(domain==='cds'&&(!cds_customer_id||client_id!=null))return res.status(400).json({error:'CDS lists require cds_customer_id and no client_id'});if(domain==='fleet'&&(!client_id||cds_customer_id!=null))return res.status(400).json({error:'Fleet lists require client_id and no cds_customer_id'});if(domain==='platform'&&(client_id!=null||cds_customer_id!=null))return res.status(400).json({error:'Platform lists cannot be customer scoped'});const out=await withOrg(req.user.org_id,async c=>{if(client_id){const q=await c.query(`SELECT id FROM cargo_clients WHERE id=$1 AND org_id=$2 AND deleted_at IS NULL`,[client_id,req.user.org_id]);if(!q.rows.length)throw httpError('client_not_found',404);}if(cds_customer_id){const q=await c.query(`SELECT id FROM cds_customers WHERE id=$1 AND org_id=$2 AND deleted_at IS NULL`,[cds_customer_id,req.user.org_id]);if(!q.rows.length)throw httpError('cds_customer_not_found',404);}const q=await c.query(`INSERT INTO communication_distribution_lists(org_id,name,description,domain,client_id,cds_customer_id,created_by) VALUES($1,$2,$3,$4,$5,$6,$7) RETURNING *`,[req.user.org_id,String(name).trim(),description,domain,client_id,cds_customer_id,actor(req)]);await audit(c,req,{action:'distribution_list.created',entityType:'distribution_list',entityId:q.rows[0].id,domain,clientId:client_id,cdsCustomerId:cds_customer_id,after:q.rows[0]});return q.rows[0];});res.status(201).json({data:out});}catch(e){next(e);}});
router.patch('/distribution-lists/:id',async(req,res,next)=>{try{const unknown=Object.keys(req.body||{}).filter(k=>!new Set(['name','description','status']).has(k));if(unknown.length)return res.status(400).json({error:`"${unknown[0]}" is not allowed`});if(req.body.status&&!LIST_STATUSES.has(req.body.status))return res.status(400).json({error:'invalid distribution list status'});const out=await withOrg(req.user.org_id,async c=>{const b=await c.query(`SELECT * FROM communication_distribution_lists WHERE id=$1 AND org_id=$2`,[req.params.id,req.user.org_id]);if(!b.rows.length)throw httpError('distribution_list_not_found',404);const u=await c.query(`UPDATE communication_distribution_lists SET name=COALESCE($2,name),description=COALESCE($3,description),status=COALESCE($4,status),updated_at=NOW() WHERE id=$1 AND org_id=$5 RETURNING *`,[req.params.id,req.body.name??null,req.body.description??null,req.body.status??null,req.user.org_id]);await audit(c,req,{action:'distribution_list.updated',entityType:'distribution_list',entityId:req.params.id,domain:b.rows[0].domain,clientId:b.rows[0].client_id,cdsCustomerId:b.rows[0].cds_customer_id,before:b.rows[0],after:u.rows[0]});return u.rows[0];});res.json({data:out});}catch(e){next(e);}});
router.post('/distribution-lists/:id/members',async(req,res,next)=>{try{const enrollmentId=req.body?.enrollment_id;const ue=uuidError(enrollmentId,'enrollment_id');if(ue)return res.status(400).json({error:ue});if(!enrollmentId)return res.status(400).json({error:'enrollment_id is required'});const out=await withOrg(req.user.org_id,async c=>{const lq=await c.query(`SELECT * FROM communication_distribution_lists WHERE id=$1 AND org_id=$2`,[req.params.id,req.user.org_id]);if(!lq.rows.length)throw httpError('distribution_list_not_found',404);const eq=await c.query(`SELECT * FROM communication_enrollments WHERE id=$1 AND org_id=$2`,[enrollmentId,req.user.org_id]);if(!eq.rows.length)throw httpError('enrollment_not_found',404);const l=lq.rows[0],en=eq.rows[0];if(l.status==='archived'||en.domain!==l.domain||(l.domain==='cds'&&en.cds_customer_id!==l.cds_customer_id)||(l.domain==='fleet'&&en.client_id!==l.client_id))throw httpError('distribution_list_scope_mismatch',409);const q=await c.query(`INSERT INTO communication_distribution_list_members(org_id,distribution_list_id,enrollment_id,added_by) VALUES($1,$2,$3,$4) ON CONFLICT(distribution_list_id,enrollment_id) DO UPDATE SET added_by=EXCLUDED.added_by RETURNING *`,[req.user.org_id,req.params.id,enrollmentId,actor(req)]);await audit(c,req,{action:'distribution_list.member_added',entityType:'distribution_list_member',entityId:q.rows[0].id,domain:l.domain,clientId:l.client_id,cdsCustomerId:l.cds_customer_id,after:q.rows[0]});return q.rows[0];});res.status(201).json({data:out});}catch(e){next(e);}});
router.delete('/distribution-lists/:id/members/:memberId',async(req,res,next)=>{try{const out=await withOrg(req.user.org_id,async c=>{const b=await c.query(`SELECT m.*,l.domain,l.client_id,l.cds_customer_id FROM communication_distribution_list_members m JOIN communication_distribution_lists l ON l.id=m.distribution_list_id WHERE m.id=$1 AND m.distribution_list_id=$2 AND m.org_id=$3`,[req.params.memberId,req.params.id,req.user.org_id]);if(!b.rows.length)throw httpError('distribution_list_member_not_found',404);await c.query(`DELETE FROM communication_distribution_list_members WHERE id=$1 AND org_id=$2`,[req.params.memberId,req.user.org_id]);await audit(c,req,{action:'distribution_list.member_removed',entityType:'distribution_list_member',entityId:req.params.memberId,domain:b.rows[0].domain,clientId:b.rows[0].client_id,cdsCustomerId:b.rows[0].cds_customer_id,before:b.rows[0]});return b.rows[0];});res.status(204).end();}catch(e){next(e);}});
router.post('/distribution-lists/:id/apply-subscriptions',async(req,res,next)=>{try{const subs=Array.isArray(req.body?.subscriptions)?req.body.subscriptions:[];if(!subs.length)return res.status(400).json({error:'subscriptions must contain at least one item'});for(const s of subs){if(!s?.event_type||!CHANNELS.has(s.channel||'email')||!MODES.has(s.delivery_mode||'immediate'))return res.status(400).json({error:'invalid subscription payload'});}const out=await withOrg(req.user.org_id,async c=>{const lq=await c.query(`SELECT * FROM communication_distribution_lists WHERE id=$1 AND org_id=$2 AND status='active'`,[req.params.id,req.user.org_id]);if(!lq.rows.length)throw httpError('distribution_list_not_found_or_inactive',404);const members=await c.query(`SELECT enrollment_id FROM communication_distribution_list_members WHERE distribution_list_id=$1 AND org_id=$2`,[req.params.id,req.user.org_id]);for(const m of members.rows)for(const s of subs)await c.query(`INSERT INTO communication_subscriptions(org_id,enrollment_id,event_type,channel,delivery_mode,enabled,critical_override) VALUES($1,$2,$3,$4,$5,$6,$7) ON CONFLICT(enrollment_id,event_type,channel) DO UPDATE SET delivery_mode=EXCLUDED.delivery_mode,enabled=EXCLUDED.enabled,critical_override=EXCLUDED.critical_override,updated_at=NOW()`,[req.user.org_id,m.enrollment_id,String(s.event_type),s.channel||'email',s.delivery_mode||'immediate',s.enabled!==false,s.critical_override!==false]);await audit(c,req,{action:'distribution_list.subscriptions_applied',entityType:'distribution_list',entityId:req.params.id,domain:lq.rows[0].domain,clientId:lq.rows[0].client_id,cdsCustomerId:lq.rows[0].cds_customer_id,metadata:{member_count:members.rows.length,subscriptions:subs}});return{member_count:members.rows.length};});res.json({data:out});}catch(e){next(e);}});
router.post('/authority/customers/:id/revoke',async(req,res,next)=>{try{const out=await withOrg(req.user.org_id,async c=>{const cq=await c.query(`SELECT id,company_name FROM cds_customers WHERE id=$1 AND org_id=$2 AND deleted_at IS NULL`,[req.params.id,req.user.org_id]);if(!cq.rows.length)throw httpError('cds_customer_not_found',404);const b=await c.query(`SELECT * FROM communication_enrollments WHERE org_id=$1 AND domain='cds' AND cds_customer_id=$2 AND status NOT IN('revoked','suspended')`,[req.user.org_id,req.params.id]);const u=await c.query(`UPDATE communication_enrollments SET status='suspended',suspended_at=COALESCE(suspended_at,NOW()),updated_at=NOW() WHERE org_id=$1 AND domain='cds' AND cds_customer_id=$2 AND status NOT IN('revoked','suspended') RETURNING id`,[req.user.org_id,req.params.id]);await audit(c,req,{action:'customer_authority.revoked',entityType:'cds_customer',entityId:req.params.id,domain:'cds',cdsCustomerId:req.params.id,before:b.rows,after:u.rows,metadata:{reason:req.body?.reason||'operator_revoke'}});return{customer:cq.rows[0],suspended:u.rows.length};});res.json({data:out});}catch(e){next(e);}});
router.post('/authority/customers/:id/restore',async(req,res,next)=>{try{const out=await withOrg(req.user.org_id,async c=>{const cq=await c.query(`SELECT id,company_name FROM cds_customers WHERE id=$1 AND org_id=$2 AND deleted_at IS NULL`,[req.params.id,req.user.org_id]);if(!cq.rows.length)throw httpError('cds_customer_not_found',404);const b=await c.query(`SELECT * FROM communication_enrollments WHERE org_id=$1 AND domain='cds' AND cds_customer_id=$2 AND status='suspended'`,[req.user.org_id,req.params.id]);const u=await c.query(`UPDATE communication_enrollments SET status='active',updated_at=NOW() WHERE org_id=$1 AND domain='cds' AND cds_customer_id=$2 AND status='suspended' RETURNING id`,[req.user.org_id,req.params.id]);await audit(c,req,{action:'customer_authority.restored',entityType:'cds_customer',entityId:req.params.id,domain:'cds',cdsCustomerId:req.params.id,before:b.rows,after:u.rows});return{customer:cq.rows[0],restored:u.rows.length};});res.json({data:out});}catch(e){next(e);}});
router.get('/authority/audit',async(req,res,next)=>{try{const params=[req.user.org_id];let filter='';if(req.query.cds_customer_id){const e=uuidError(req.query.cds_customer_id,'cds_customer_id');if(e)return res.status(400).json({error:e});params.push(req.query.cds_customer_id);filter=' AND (scope_cds_customer_id=$2 OR entity_id=$2)';}const q=await withOrg(req.user.org_id,c=>c.query(`SELECT id,actor_user_id,action,entity_type,entity_id,scope_domain,scope_client_id,scope_cds_customer_id,metadata,created_at FROM communication_authority_audit WHERE org_id=$1${filter} ORDER BY created_at DESC LIMIT 200`,params));res.json({data:q.rows});}catch(e){next(e);}});
router.get('/customers',async(req,res,next)=>{try{const q=await withOrg(req.user.org_id,c=>c.query(`SELECT id,code,company_name,contact_person,contact_person AS contact_name,email,phone,status FROM cds_customers WHERE org_id=$1 AND deleted_at IS NULL ORDER BY lower(company_name),lower(COALESCE(contact_person,''))`,[req.user.org_id]));res.json({data:q.rows});}catch(e){next(e);}});
module.exports=router;
