const router = require('express').Router();
const { authenticate, authorize } = require('../middleware/auth');
const { attachOrgDb, withOrg } = require('../utils/orgScopedDb');
const { getQueues } = require('../config/queue');
const logger = require('../utils/logger');
const crypto = require('crypto');
const { buildManifestWorkbook, isActiveRow } = require('../services/email/clientPulseWorkbook.service');
const { queueClientPulseEmail } = require('../services/email/email.service');
const { generateAndQueueScopedClientPulse, listCustomerPulseTargets } = require('../services/email/scopedClientPulse.service');
router.use(authenticate, authorize('admin'), attachOrgDb);
router.get('/queues', async (req, res, next) => { try { const queues=getQueues(); const stats={}; for(const [name,q] of Object.entries(queues)){ if(!q){stats[name]=null;continue;} try{const [waiting,active,failed,completed]=await Promise.all([q.getWaitingCount(),q.getActiveCount(),q.getFailedCount(),q.getCompletedCount()]);const deadJobs=await q.getFailed(0,20);stats[name]={waiting,active,failed,completed,dead:deadJobs.map(j=>({id:j.id,name:j.name,failedReason:j.failedReason,attemptsMade:j.attemptsMade,timestamp:j.timestamp}))};}catch(err){stats[name]={error:err.message};}} res.json({data:stats});}catch(err){next(err);} });
router.get('/cds-client-pulse/runs', async (req,res,next)=>{try{const limit=Math.min(Math.max(Number(req.query.limit)||20,1),100);const result=await withOrg(req.user.org_id,client=>client.query(`SELECT id,snapshot_at,status,active_booking_count,row_count,attachment_name,manifest_hash,email_notification_ids,error,created_at,updated_at FROM cds_client_pulse_runs WHERE org_id=$1 ORDER BY created_at DESC LIMIT $2`,[req.user.org_id,limit]));res.json({data:result.rows});}catch(err){next(err);}});
function manifestRow(row){return{booking_number:row.booking_number,carrier_reference:row.carrier_reference,vessel:row.vessel,file_reference:row.file_reference,controller:row.controller,commodity:row.commodity,packing_list_no:row.packing_list_no,container_number:row.container_number,iso_type:row.iso_type,seal_number:row.seal_number,seal_number_2:row.seal_number_2,status:row.status,clamped_at:row.clamped_at,clamped_at_t:row.clamped_at,unclamped_at:row.unclamped_at,lock_number:row.lock_number||row.lock_serial,terminal:row.terminal,yard_status:row.yard_status,transporter:row.transporter,horse_reg:row.horse_reg||row.horse_reg_derived,trailer_reg:row.trailer_reg,driver_name:row.driver_name||row.driver_name_derived,driver_contact:row.driver_contact||row.driver_contact_derived,invoiced:row.invoiced?'YES':'NO'};}
function pulseStamp(date){const parts=new Intl.DateTimeFormat('en-CA',{timeZone:process.env.CDS_CLIENT_PULSE_TIMEZONE||'Africa/Nairobi',year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:false}).formatToParts(date).reduce((o,p)=>(o[p.type]=p.value,o),{});return `${parts.year}-${parts.month}-${parts.day}_${parts.hour}-${parts.minute}-${parts.second}`;}
async function queueManualSuperAdminPulse(orgId,snapshotAt){const recipients=await withOrg(orgId,client=>client.query(`SELECT DISTINCT email,name FROM client_email_recipients WHERE org_id=$1 AND authority_role='super_admin' AND enabled=true AND deleted_at IS NULL AND email IS NOT NULL ORDER BY email`,[orgId]));if(!recipients.rows.length)return{skipped:true,reason:'no_super_admin_recipients',queued:0};const run=await withOrg(orgId,client=>client.query(`INSERT INTO cds_client_pulse_runs (org_id,snapshot_at,status,idempotency_key) VALUES ($1,$2,'generating',$3) ON CONFLICT (org_id,idempotency_key) DO NOTHING RETURNING id`,[orgId,snapshotAt,`cds-client-pulse:super-admin:${snapshotAt.toISOString().slice(0,13)}`]));if(!run.rows.length)return{skipped:true,reason:'duplicate_snapshot',queued:0};const runId=run.rows[0].id;try{const{rows}=await withOrg(orgId,client=>client.query(`SELECT bc.id,bc.booking_id,bc.container_number,bc.seal_number,bc.seal_number_2,bc.packing_list_no,bc.iso_type,bc.weight_kg,bc.status,bc.notes,bc.clamped_at,bc.unclamped_at,bc.terminal,bc.yard_status,bc.invoiced,bc.lock_number,bc.transporter_name,bc.horse_reg,bc.trailer_reg,bc.driver_name,bc.driver_contact,b.booking_number,b.reference AS file_reference,b.vessel,b.commodity,b.controller,b.country_code,b.direction,b.carrier_reference,b.status AS booking_status,b.pickup_location,b.delivery_location,b.eta,b.created_at AS booking_created_at,t.trip_number,t.status AS trip_status,COALESCE(bc.transporter_name,tr.company_name) AS transporter,COALESCE(bc.horse_reg,v.registration) AS horse_reg_derived,COALESCE(bc.driver_name,d.name) AS driver_name_derived,COALESCE(bc.driver_contact,d.phone) AS driver_contact_derived,l.serial AS lock_serial FROM cds_booking_containers bc JOIN cds_bookings b ON b.id=bc.booking_id LEFT JOIN cds_trips t ON t.id=bc.trip_id LEFT JOIN cds_transporters tr ON tr.id=t.transporter_id LEFT JOIN cds_vehicles v ON v.id=t.vehicle_id LEFT JOIN cds_drivers d ON d.id=t.driver_id LEFT JOIN cds_electronic_locks l ON l.id=t.lock_id WHERE bc.org_id=$1 AND bc.deleted_at IS NULL AND b.org_id=$1 AND b.deleted_at IS NULL ORDER BY b.created_at DESC NULLS LAST,b.id,bc.id`,[orgId]));const active=rows.filter(isActiveRow).map(manifestRow);const activeBookingCount=new Set(active.map(r=>r.booking_number).filter(Boolean)).size;logger.info(`CDS Client Pulse global run=${runId} activeRows=${active.length} activeBookings=${activeBookingCount} recipients=${recipients.rows.length}`);if(!active.length){await withOrg(orgId,client=>client.query(`UPDATE cds_client_pulse_runs SET status='skipped',active_booking_count=0,row_count=0,updated_at=NOW() WHERE id=$1`,[runId]));return{runId,skipped:true,reason:'no_active_bookings',queued:0};}const workbook=await buildManifestWorkbook(active,snapshotAt,{scopeLabel:'GLOBAL'});const manifestHash=crypto.createHash('sha256').update(workbook).digest('hex');const filename=`Super_Admin_Client_Dispatch_Master_Active_Bookings_${pulseStamp(snapshotAt)}_EAT.xlsx`;const result=await queueClientPulseEmail({orgId,recipients:recipients.rows.map(r=>r.email),snapshotAt:snapshotAt.toISOString(),activeBookingCount,dateLabel:new Intl.DateTimeFormat('en-GB',{timeZone:process.env.CDS_CLIENT_PULSE_TIMEZONE||'Africa/Nairobi',day:'2-digit',month:'short',year:'numeric'}).format(snapshotAt),attachment:{filename,content:workbook.toString('base64'),contentType:'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'},correlationId:`cds-client-pulse:${runId}`,idempotencyKey:`cds-client-pulse:super-admin:${snapshotAt.toISOString().slice(0,13)}`});await withOrg(orgId,client=>client.query(`UPDATE cds_client_pulse_runs SET status=$2,active_booking_count=$3,row_count=$4,attachment_name=$5,manifest_hash=$6,updated_at=NOW() WHERE id=$1`,[runId,result.queued?'queued':'skipped',activeBookingCount,active.length,filename,manifestHash]));logger.info(`CDS Client Pulse global queued run=${runId} queued=${result.queued} duplicate=${result.duplicate} filename=${filename}`);return{runId,queued:result.queued,duplicate:result.duplicate,rows:active.length,recipients:recipients.rows.length,filename};}catch(err){await withOrg(orgId,client=>client.query(`UPDATE cds_client_pulse_runs SET status='failed',error=$2,updated_at=NOW() WHERE id=$1`,[runId,String(err.message||err).slice(0,4000)]));logger.error(`Manual Super Admin Client Pulse failed: org=${orgId} run=${runId} error=${err.message}`);throw err;}}
router.post('/cds-client-pulse/send',async(req,res,next)=>{try{const snapshotAt=new Date();const global=await queueManualSuperAdminPulse(req.user.org_id,snapshotAt);const customerIds=await listCustomerPulseTargets(req.user.org_id);const customers=[];for(const customerId of customerIds){try{customers.push(await generateAndQueueScopedClientPulse(req.user.org_id,customerId,{snapshotAt,reason:'manual_scoped'}));}catch(err){customers.push({customerId,skipped:true,reason:'delivery_failed',error:err.message});}}const queued=(global.queued||0)+customers.reduce((n,r)=>n+(r.queued||0),0);logger.info(`CDS Client Pulse dispatch complete snapshot=${snapshotAt.toISOString()} queued=${queued} globalQueued=${global.queued||0} customerTargets=${customerIds.length}`);res.status(202).json({data:{queued,global,customers}});}catch(err){next(err);}});
module.exports=router;
