const crypto = require('crypto');
const { withOrg } = require('../../utils/orgScopedDb');
const logger = require('../../utils/logger');
const { queueClientPulseEmail } = require('./email.service');
const { buildManifestWorkbook, isActiveRow } = require('./clientPulseWorkbook.service');
const orgQuery = (orgId, text, params) => withOrg(orgId, client => client.query(text, params));
function dateLabel(date) { return new Intl.DateTimeFormat('en-GB', { timeZone: process.env.CDS_CLIENT_PULSE_TIMEZONE || 'Africa/Nairobi', day: '2-digit', month: 'short', year: 'numeric' }).format(date); }
function manifestRow(row) { return { booking_number: row.booking_number, carrier_reference: row.carrier_reference, vessel: row.vessel, file_reference: row.file_reference, controller: row.controller, commodity: row.commodity, packing_list_no: row.packing_list_no, container_number: row.container_number, iso_type: row.iso_type, seal_number: row.seal_number, seal_number_2: row.seal_number_2, status: row.status, clamped_at: row.clamped_at, clamped_at_t: row.clamped_at, unclamped_at: row.unclamped_at, lock_number: row.lock_number || row.lock_serial, terminal: row.terminal, yard_status: row.yard_status, transporter: row.transporter, horse_reg: row.horse_reg || row.horse_reg_derived, trailer_reg: row.trailer_reg, driver_name: row.driver_name || row.driver_name_derived, driver_contact: row.driver_contact || row.driver_contact_derived, invoiced: row.invoiced ? 'YES' : 'NO' }; }
async function fetchCustomerManifestSnapshot(orgId, customerId) { return withOrg(orgId, client => client.query(`SELECT bc.id,bc.booking_id,bc.container_number,bc.seal_number,bc.seal_number_2,bc.packing_list_no,bc.iso_type,bc.weight_kg,bc.status,bc.notes,bc.clamped_at,bc.unclamped_at,bc.terminal,bc.yard_status,bc.invoiced,bc.lock_number,bc.transporter_name,bc.horse_reg,bc.trailer_reg,bc.driver_name,bc.driver_contact,b.customer_id,b.booking_number,b.reference AS file_reference,b.vessel,b.commodity,b.controller,b.country_code,b.direction,b.carrier_reference,b.status AS booking_status,b.pickup_location,b.delivery_location,b.eta,b.created_at AS booking_created_at,cu.company_name AS customer_name,t.trip_number,t.status AS trip_status,COALESCE(bc.transporter_name,tr.company_name) AS transporter,COALESCE(bc.horse_reg,v.registration) AS horse_reg_derived,COALESCE(bc.driver_name,d.name) AS driver_name_derived,COALESCE(bc.driver_contact,d.phone) AS driver_contact_derived,l.serial AS lock_serial FROM cds_booking_containers bc JOIN cds_bookings b ON b.id=bc.booking_id JOIN cds_customers cu ON cu.id=b.customer_id AND cu.org_id=$1 LEFT JOIN cds_trips t ON t.id=bc.trip_id LEFT JOIN cds_transporters tr ON tr.id=t.transporter_id LEFT JOIN cds_vehicles v ON v.id=t.vehicle_id LEFT JOIN cds_drivers d ON d.id=t.driver_id LEFT JOIN cds_electronic_locks l ON l.id=t.lock_id WHERE bc.org_id=$1 AND bc.deleted_at IS NULL AND b.org_id=$1 AND b.deleted_at IS NULL AND b.customer_id=$2 ORDER BY b.created_at DESC NULLS LAST,b.id,bc.id`, [orgId, customerId])); }
async function resolveCustomerPulseRecipients(orgId, customerId, { includeGlobalAuthority = false } = {}) { return withOrg(orgId, client => client.query(`SELECT DISTINCT r.email,r.name,e.id AS enrollment_id,s.id AS subscription_id FROM communication_enrollments e JOIN client_email_recipients r ON r.id=e.recipient_id JOIN communication_subscriptions s ON s.enrollment_id=e.id WHERE e.org_id=$1 AND e.domain='cds' AND e.cds_customer_id=$2 AND e.status IN ('verified','active') AND r.org_id=$1 AND r.enabled=true AND r.deleted_at IS NULL AND s.org_id=$1 AND s.event_type='cds.client_pulse' AND s.channel='email' AND s.enabled=true ${includeGlobalAuthority ? "UNION SELECT DISTINCT r.email,r.name,NULL AS enrollment_id,NULL AS subscription_id FROM client_email_recipients r WHERE r.org_id=$1 AND r.authority_role='super_admin' AND r.enabled=true AND r.deleted_at IS NULL AND r.email IS NOT NULL" : ""} ORDER BY email`, [orgId, customerId])); }
async function generateAndQueueScopedClientPulse(orgId, customerId, { snapshotAt = new Date(), reason = 'manual' } = {}) { if (!orgId || !customerId) throw new Error('orgId and customerId are required for scoped Client Pulse'); const snapshot = snapshotAt instanceof Date ? snapshotAt : new Date(snapshotAt); if (Number.isNaN(snapshot.getTime())) throw new Error('Invalid Client Pulse snapshot time'); const idempotencyKey = `cds-client-pulse:${customerId}:${snapshot.toISOString().slice(0,13)}`; const claim = await withOrg(orgId, client => client.query(`INSERT INTO cds_client_pulse_runs (org_id,snapshot_at,status,idempotency_key) VALUES ($1,$2,'generating',$3) ON CONFLICT (org_id,idempotency_key) DO NOTHING RETURNING id`, [orgId, snapshot, idempotencyKey])); if (!claim.rows.length) return { skipped: true, reason: 'duplicate_snapshot', customerId }; const runId = claim.rows[0].id; try { const customer = await withOrg(orgId, client => client.query(`SELECT id,company_name FROM cds_customers WHERE id=$1 AND org_id=$2 AND deleted_at IS NULL LIMIT 1`, [customerId, orgId])); if (!customer.rows.length) throw new Error('CDS customer not found in organization'); const customerName = String(customer.rows[0].company_name || 'Customer').trim() || 'Customer'; const active = (await fetchCustomerManifestSnapshot(orgId, customerId)).rows.filter(isActiveRow).map(manifestRow); const recipients = await resolveCustomerPulseRecipients(orgId, customerId, { includeGlobalAuthority: reason !== 'scheduled' }); const activeBookingCount = new Set(active.map(r => r.booking_number).filter(Boolean)).size; if (!active.length) { await orgQuery(orgId, `UPDATE cds_client_pulse_runs SET status='skipped',active_booking_count=$2,row_count=$3,updated_at=NOW() WHERE id=$1`, [runId, activeBookingCount, 0]); return { skipped: true, reason: 'no_active_bookings', customerId }; } if (!recipients.length) { await orgQuery(orgId, `UPDATE cds_client_pulse_runs SET status='skipped',active_booking_count=$2,row_count=$3,updated_at=NOW() WHERE id=$1`, [runId, activeBookingCount, active.length]); return { skipped: true, reason: 'no_enrolled_recipients', customerId, rows: active.length }; } const workbook = await buildManifestWorkbook(active, snapshot, { scopeLabel: customerName }); const manifestHash = crypto.createHash('sha256').update(workbook).digest('hex'); const stamp = snapshot.toISOString().replace(/[:]/g, '').replace(/\.\d{3}Z$/, 'Z'); const filename = `CDS_Client_Pulse_${customerName.replace(/[^A-Za-z0-9_-]+/g, '_')}_${stamp}_EAT.xlsx`; const result = await queueClientPulseEmail({ orgId, recipients: recipients.map(r => r.email), snapshotAt: snapshot.toISOString(), activeBookingCount, dateLabel: dateLabel(snapshot), attachment: { filename, content: workbook.toString('base64'), contentType: 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet' }, correlationId: `cds-client-pulse:${runId}`, idempotencyKey }); const ids = await withOrg(orgId, client => client.query(`SELECT id FROM email_notifications WHERE org_id=$1 AND notification_type='cds_client_pulse' AND correlation_id=$2 ORDER BY created_at DESC`, [orgId, `cds-client-pulse:${runId}`])); await orgQuery(orgId, `UPDATE cds_client_pulse_runs SET status=$2,active_booking_count=$3,row_count=$4,attachment_name=$5,manifest_hash=$6,email_notification_ids=$7,updated_at=NOW() WHERE id=$1`, [runId, result.queued ? 'queued' : 'skipped', activeBookingCount, active.length, filename, manifestHash, ids.rows.map(r => r.id)]); await withOrg(orgId, client => client.query(`INSERT INTO communication_delivery_events (org_id,enrollment_id,subscription_id,event_type,channel,status,correlation_id,payload_hash,sent_at) SELECT $1,e.id,s.id,'cds.client_pulse','email',$2,$3,$4,CASE WHEN $2='sent' THEN NOW() ELSE NULL END FROM communication_enrollments e JOIN communication_subscriptions s ON s.enrollment_id=e.id WHERE e.org_id=$1 AND e.domain='cds' AND e.cds_customer_id=$5 AND e.status IN ('verified','active') AND s.event_type='cds.client_pulse' AND s.channel='email' AND s.enabled=true`, [orgId, result.queued ? 'queued' : 'skipped', `cds-client-pulse:${runId}`, manifestHash, customerId])); return { runId, queued: result.queued, duplicate: result.duplicate, rows: active.length, recipients: recipients.length, customerId, filename, manifestHash, reason }; } catch (err) { await orgQuery(orgId, `UPDATE cds_client_pulse_runs SET status='failed',error=$2,updated_at=NOW() WHERE id=$1`, [runId, String(err.message || err).slice(0, 4000)]); logger.error(`Scoped CDS Client Pulse failed: org=${orgId} customer=${customerId} run=${runId} error=${err.message}`); throw err; } }
async function listCustomerPulseTargets(orgId) { const result = await withOrg(orgId, client => client.query(`SELECT DISTINCT e.cds_customer_id FROM communication_enrollments e JOIN communication_subscriptions s ON s.enrollment_id=e.id JOIN client_email_recipients r ON r.id=e.recipient_id WHERE e.org_id=$1 AND e.domain='cds' AND e.status IN ('verified','active') AND r.enabled=true AND r.deleted_at IS NULL AND s.event_type='cds.client_pulse' AND s.channel='email' AND s.enabled=true`, [orgId])); return result.rows.map(r => r.cds_customer_id).filter(Boolean); }
module.exports = { fetchCustomerManifestSnapshot, resolveCustomerPulseRecipients, generateAndQueueScopedClientPulse, listCustomerPulseTargets };
