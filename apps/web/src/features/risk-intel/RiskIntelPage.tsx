import { useState, useEffect } from 'react'
import { useQueryClient } from '@tanstack/react-query'
import { useAuthStore } from '../../stores/auth.js'
import { api } from '../../lib/api.js'
import { useRiskZones } from './hooks/useRiskZones.js'
import { useRiskRealtime } from './hooks/useRiskRealtime.js'
import { levelColor } from './utils/colors.js'
import ContinentBar from './components/ContinentBar.js'
import LiveTicker from './components/LiveTicker.js'
import LiveFeedPanel from './components/LiveFeedPanel.js'
import ThreatList from './components/ThreatList.js'
import RiskMap from './components/RiskMap.js'
import DetailDrawer from './components/DetailDrawer.js'
import IntelligenceOperations from './components/IntelligenceOperations.js'
import type { Continent, RiskLevel } from './types/risk.js'
function useIsMobile(){const [mobile,setMobile]=useState(()=>typeof window!=='undefined'&&window.innerWidth<1024);useEffect(()=>{const fn=()=>setMobile(window.innerWidth<1024);window.addEventListener('resize',fn);return()=>window.removeEventListener('resize',fn)},[]);return mobile}
export default function RiskIntelPage(){const queryClient=useQueryClient();const user=useAuthStore(s=>s.user);const orgId=user?.org_id??'';const isMobile=useIsMobile();const [activeCont,setActiveCont]=useState<Continent>('global');const [levelFilter,setLevelFilter]=useState<RiskLevel|'all'>('all');const [activeZoneId,setActiveZoneId]=useState<string|null>(null);const [heatVisible,setHeatVisible]=useState(true);const [activeTab,setActiveTab]=useState<'map'|'list'|'feed'>('list');const [refreshState,setRefreshState]=useState<'idle'|'refreshing'|'done'>('idle');const {data,isLoading}=useRiskZones(activeCont,levelFilter);const zones=data?.zones??[];const counts=data?.counts??{high:0,medium:0,low:0,total:0};useRiskRealtime(orgId,queryClient);const activeZone=zones.find(z=>z.id===activeZoneId)??null;const handleExport=async()=>{const params=new URLSearchParams();if(activeCont!=='global')params.set('continent',activeCont);if(levelFilter!=='all')params.set('level',levelFilter);const res=await api.get(`/risk/export?${params}`,{responseType:'blob'});const url=URL.createObjectURL(res.data as Blob);const a=document.createElement('a');a.href=url;a.download=`risk-intel-${new Date().toISOString().slice(0,10)}.csv`;a.click();URL.revokeObjectURL(url)};const handleRefreshIntel=async()=>{if(refreshState==='refreshing')return;setRefreshState('refreshing');try{await api.post('/risk/refresh-intel')}catch{}setTimeout(()=>{queryClient.invalidateQueries({queryKey:['risk-zones']});queryClient.invalidateQueries({queryKey:['risk-ticker']});queryClient.invalidateQueries({queryKey:['risk-live-feed']});setRefreshState('done');setTimeout(()=>setRefreshState('idle'),3000)},25000)};return <div style={{display:'flex',flexDirection:'column',height:'100%',background:'#0a1220',color:'#e8e6df',fontFamily:'Inter,system-ui,sans-serif',overflow:'auto'}}><div style={{display:'flex',alignItems:'center',justifyContent:'space-between',padding:isMobile?'8px 12px':'10px 16px',borderBottom:'1px solid rgba(255,255,255,.07)',flexShrink:0,background:'#0d1520',gap:8,flexWrap:'wrap'}}><div style={{display:'flex',alignItems:'center',gap:8}}><div style={{width:7,height:7,borderRadius:'50%',background:'#E24B4A',boxShadow:'0 0 6px #E24B4A'}}/><span style={{fontFamily:'IBM Plex Mono,monospace',fontSize:isMobile?11:12,fontWeight:700,letterSpacing:'.1em',textTransform:'uppercase',color:'#F0B429'}}>Risk Intel</span></div><div style={{display:'flex',gap:isMobile?8:14,alignItems:'center'}}>{!isLoading&&(['high','medium','low'] as RiskLevel[]).map(level=><div key={level} style={{display:'flex',alignItems:'center',gap:4}}><span style={{width:6,height:6,borderRadius:'50%',background:levelColor(level)}}/><span style={{fontFamily:'IBM Plex Mono,monospace',fontSize:11,color:levelColor(level)}}>{counts[level]}</span>{!isMobile&&<span style={{fontSize:9,color:'#4e4c44',textTransform:'capitalize'}}>{level}</span>}</div>)}{!isLoading&&<span style={{fontFamily:'IBM Plex Mono,monospace',fontSize:11,color:'#6e6c64'}}>/{counts.total}</span>}</div><div style={{display:'flex',gap:6}}><button onClick={()=>setHeatVisible(v=>!v)} style={{padding:'3px 8px',background:heatVisible?'rgba(240,180,41,.12)':'rgba(255,255,255,.04)',border:'1px solid rgba(255,255,255,.08)',borderRadius:5,cursor:'pointer',fontSize:10,color:heatVisible?'#F0B429':'#6e6c64'}}>Heat</button>{!isMobile&&<button onClick={handleExport} style={{padding:'3px 8px',background:'rgba(255,255,255,.04)',border:'1px solid rgba(255,255,255,.08)',borderRadius:5,cursor:'pointer',fontSize:10,color:'#9a9890'}}>CSV</button>}{user?.role==='admin'&&<button onClick={handleRefreshIntel} disabled={refreshState==='refreshing'} style={{padding:'3px 8px',background:refreshState==='done'?'rgba(76,175,80,.12)':'rgba(255,255,255,.04)',border:'1px solid rgba(255,255,255,.08)',borderRadius:5,cursor:'pointer',fontSize:10,color:refreshState==='done'?'#4CAF50':'#9a9890'}}>{refreshState==='refreshing'?'Refreshing…':refreshState==='done'?'Refreshed ✓':'Refresh Intel'}</button>}</div></div><LiveTicker continent={activeCont}/><ContinentBar active={activeCont} zones={zones} onChange={c=>{setActiveCont(c);if(isMobile)setActiveTab('map')}}/>{isMobile&&<div style={{display:'flex',borderBottom:'1px solid rgba(255,255,255,.06)',flexShrink:0}}>{(['list','map','feed'] as const).map(t=><button key={t} onClick={()=>setActiveTab(t)} style={{flex:1,padding:8,background:'none',border:'none',borderBottom:activeTab===t?'2px solid #F0B429':'2px solid transparent',cursor:'pointer',fontSize:12,fontWeight:600,color:activeTab===t?'#F0B429':'#6e6c64',textTransform:'capitalize'}}>{t==='list'?`Zones (${zones.length})`:t==='map'?'Map':'Live Feed'}</button>)}</div>}<div style={{height:isMobile?'55vh':420,display:'flex',overflow:'hidden',position:'relative',flexShrink:0}}><div style={{width:isMobile?'100%':280,borderRight:isMobile?'none':'1px solid rgba(255,255,255,.06)',display:(!isMobile||activeTab==='list')?'flex':'none',flexDirection:'column',overflow:'hidden',flexShrink:0}}><ThreatList zones={zones} levelFilter={levelFilter} activeId={activeZoneId} onSelect={id=>setActiveZoneId(id===activeZoneId?null:id)} onLevelChange={setLevelFilter}/></div><div style={{flex:1,position:'relative',overflow:'hidden',display:(!isMobile||activeTab==='map')?'flex':'none',flexDirection:'column'}}>{isLoading&&<div style={{position:'absolute',inset:0,display:'flex',alignItems:'center',justifyContent:'center',zIndex:5,pointerEvents:'none'}}><span style={{fontSize:12,color:'#4e4c44'}}>Loading threat data…</span></div>}<RiskMap zones={zones} activeCont={activeCont} activeZoneId={activeZoneId} heatVisible={heatVisible} onZoneClick={id=>{setActiveZoneId(id);if(isMobile)setActiveTab('map')}}/></div><div style={{width:isMobile?'100%':320,borderLeft:isMobile?'none':'1px solid rgba(255,255,255,.06)',display:(!isMobile||activeTab==='feed')?'flex':'none',flexDirection:'column',overflow:'hidden',flexShrink:0,background:'#0a1220'}}><LiveFeedPanel continent={activeCont}/></div><DetailDrawer zone={activeZone} onClose={()=>setActiveZoneId(null)} isMobile={isMobile}/></div>{user?.role==='admin'&&<IntelligenceOperations/>}</div>}
