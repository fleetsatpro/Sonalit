// Optional production preload hook referenced by the Vercel environment.
// Keep this module side-effect safe: production must remain bootable even when
// no runtime patch is required.
'use strict';
module.exports = {};
