apiVersion: v1
kind: Service
metadata:
  name: {{ include "sonalit-base.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sonalit-base.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "sonalit-base.selectorLabels" . | nindent 4 }}
